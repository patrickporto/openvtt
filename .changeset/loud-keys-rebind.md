---
	'@openvtt/hotkeys': minor
---

Add `@openvtt/hotkeys`: declarative, context-aware hotkey management with rebindable keymaps, conflict detection, layout-independent matching (`event.code`), Valibot-validated profile persistence and full `@openvtt/events` integration (`hotkeyTriggered`, `bindsChanged`, `contextsChanged`, `hotkeyError` plus a `beforeHotkey` veto hook).
